import { Button } from "@/components/ui/button";
import Image from "next/image";

export default function Home() {
  return (
  <div>
    <p>"Thus a board shall be(!)"</p>
    <Button>So let it be.</Button>
  </div>
  );
}
